import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  button: {
    borderWidth: 1,
    borderColor: 'gray',
    padding: 10,
    borderRadius: 5,
  },
  title: {
    color: 'gray',
    fontSize: 16,
    textAlign: 'center',
  },
});

export default styles;
